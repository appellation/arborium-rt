{
  "name": "@discord/arborium-rt-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Dev CLI for building @discord/arborium-rt and its grammar subpath modules. Not published — run via tsx.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  },
  "engines": {
    "node": ">=20"
  }
}
