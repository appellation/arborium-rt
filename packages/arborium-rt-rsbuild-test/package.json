{
  "name": "@discord/arborium-rt-rsbuild-test",
  "version": "0.0.0",
  "private": true,
  "description": "Integration test harness. Builds a tiny rspack-powered app that imports @discord/arborium-rt to verify the package bundles cleanly (no `import(\"module\")` leaks, GRAMMARS URL assets resolved).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "rsbuild build",
    "dev": "rsbuild dev",
    "test": "rsbuild build"
  },
  "dependencies": {
    "@discord/arborium-rt": "workspace:*"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.0.0",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
